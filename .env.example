# Example .env file
# Copy this to .env and fill in your values if they differ from defaults

# URL for the Ollama API for generating responses (including image analysis)
# Default is http://localhost:11434/api/generate if not specified in app.py
# OLLAMA_API_URL=http://localhost:11434/api/generate

# URL for listing Ollama models
# Default is http://localhost:11434/api/tags if not specified in app.py
# OLLAMA_LIST_MODELS_URL=http://localhost:11434/api/tags
